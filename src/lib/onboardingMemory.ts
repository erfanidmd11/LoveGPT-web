// src/lib/onboardingMemory.ts

const onboardingMemory = {
  coreValuesRanked: [] as { key: string; label: string }[],
  personalityTraits: [] as string[], // ✅ required for Step11
};

export default onboardingMemory;
